import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    width: "100%",
    height: 150,
    marginTop: 20,
    resizeMode: "contain",
  },
  content: {
    marginHorizontal: 40,
  },
});
