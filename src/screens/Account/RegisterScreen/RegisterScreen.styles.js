import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  img: {
    width: "100%",
    height: 150,
    resizeMode: "contain",
    marginTop: 20,
  },
  viewRegister: {
    marginTop: 30,
    marginHorizontal: 20,
  },
});
